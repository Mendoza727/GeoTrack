import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    btnPrimary: {
        backgroundColor: 'blue',
        padding: 10,
        borderRadius: 100,
        margin: 10,
        width: 200,
        alignItems: 'center'
    }
})